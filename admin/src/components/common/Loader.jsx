import React from "react";

const Loader = () => (
    <div className="flex items-center justify-center h-full">
        <div className="w-12 h-12 border-b-4 border-red-600 rounded-full animate-spin"></div>
    </div>
);

export default Loader;
