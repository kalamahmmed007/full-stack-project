import React from "react";

const AdminNavbar = () => {
    return (
        <nav className="flex items-center justify-between px-6 py-3 text-white shadow-md bg-dark">
            <div className="font-bold text-red-600">Logo</div>
            <div>Notifications / Profile</div>
        </nav>
    );
};

export default AdminNavbar;
