import React from "react";

const AdminNavbar = () => {
    return (
        <nav className="text-dark bg-dark flex items-center justify-between px-6 py-3 shadow-md">
            <div className="font-bold text-red-600">Logo</div>
            <div>Notifications / Profile</div>
        </nav>
    );
};

export default AdminNavbar;
